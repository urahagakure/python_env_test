{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "ruff:check",
      "type": "shell",
      "command": "poetry run ruff check --config pyproject.toml --force-exclude .",
      "options": { "cwd": "${workspaceFolder}" },
      "group": { "kind": "build", "isDefault": true }
    }
  ]
}